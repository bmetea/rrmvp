/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'whimsical-leader-0be11697b0.media.strapiapp.com'
    ],
  },
}

module.exports = nextConfig 